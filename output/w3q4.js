/*Q4:  Create a function called `displayNames( )`. 
	Define a String variable called `friendsName`. 
	Put one of your friend's name in the variable. 
	The function should use an alert to display the variable
	`yourName` which is passes as an argument in the function. 
	It should then display `friendsName` within an alert.
*/

var friendsName = "Farook";
function displayNames(yourName){
	// alert(yourName);
	// console.log(yourName);
}
displayNames(friendsName);

module.exports = friendsName;